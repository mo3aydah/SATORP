# Devlog

This directory holds devlog entries for resolved issues, per the Memory Bank guide.

- **Naming:** `YYYYMMDD-title.mdc` (e.g. `20250215-fix-canvas-rtl.mdc`).
- **When:** Create a new file each time a bug or operational issue is definitively fixed.
- **Template:** See the Memory Bank guide (devlog section) for the required sections: Problem Description, Error Messages, Root Cause(s), Solution Steps, Verification, Optional Future Improvements.

No devlog entries yet.
